module.exports = class myPlugin {
    /**
     * Handle the plugin action
     * 
     * @param {Discord.client} client 
     */
    static handle(client){
        // commands that you need to write
        // Just for the example :
        console.log('Example plugin has been executed');
    }
}